// require Database plugin like Sequelize or Mongoose

// Logic to define model

// For testing purposes mocking data
const User = {
  getUser: function () {
    return [{ name: "AK" }, { name: "Revanth" }];
  },
};

module.exports = User;
